# Change log — 2026-04-13

Convention: one file per calendar day, name `CHANGELOG/YYYY-MM-DD.md`. Newest entries at the **top** (below this heading).

---

## Daily next-time handoff + rule

- **Files:** `.cursorrules`, `CHANGELOG/NEXT-TIME-2026-04-13.md`
- **What:** Added a persistent rule requiring daily creation/maintenance of a "next time" TODO handoff file and created today's handoff with deferred physics-rule-engine tasks.
- **Why:** Ensure unresolved discussion items are carried forward and actively checked in future sessions.

## Wall FSM moved under JSON rule order

- **Files:** `Sandbox/index.html`, `Sandbox/rules/triangle-physics.rules.v1.json`
- **What:** Replaced hardcoded wall-mode precedence with rule-selected candidates (`wallSlideComposite`, `slideLocal`, `pivotRotate`) and added rule `forbids` support so non-wall states can be explicitly blocked while wall mode is active.
- **Why:** Ensure wall FSM behavior also changes when rule JSON changes, removing remaining fixed in-code state ordering.

## JSON-driven physics state engine + triangle sight

- **Files:** `Sandbox/index.html`, `Sandbox/rules/triangle-physics.rules.v1.json`, `Sandbox/media-bridge.cjs`, `Sandbox/run-physics-tests.cjs`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/send-play-command.cjs`, `Sandbox/README.txt`
- **What:** Added a neighbor-aware triangle perception model (`buildTriangleSight`) and a rule interpreter that selects movement phases from JSON-defined state order (`lane`, blocked-lane composite/flip, bottom flip, wall, interior). Added runtime loading for physics rules JSON through bridge action/endpoint, automation hooks, and test-runner support (`rulesFile`).
- **Why:** Move movement behavior toward declarative, editable JSON rules while ensuring each triangle decision is based on current lit-neighbor visibility and local geometric context at every tick.

## Special blocked-lane edge case correction

- **Files:** `Sandbox/index.html`
- **What:** Updated down-pointy blocked-lane handling to use a composite move (`downPointyBlockedComposite`) that lands directly on the next same-kind lane cell when available.
- **Why:** Enforce the requested special-case behavior so a blocked drop transitions `...268 -> 282 -> 286` instead of detouring through unrelated near-bottom rotations.
- **What (follow-up):** Refined trigger to fire only when the immediate next same-kind lane target is occupied, keeping the decision based on local occupancy and geometric lane ordering rather than ID/row/container heuristics.

## Down-pointy blocked-lane flip preference

- **Files:** `Sandbox/index.html`
- **What:** Added a targeted edge-case branch so down-pointy pieces with no available same-kind lane step prefer a direct downhill opposite-kind flip (`downPointyBlockedFlip`) before generic interior tie-break rotation.
- **Why:** Reduce counterintuitive near-bottom detours when lane continuation is blocked by settled pieces.

## Second-drop phase trace tooling

- **Files:** `Sandbox/index.html`
- **What:** Added `__TG_AUTOMATION__.previewNextTick()` debug helper (plus build bump) to inspect exact next-tick move phases without mutating board state.
- **Why:** Support precise diagnosis of visually surprising movement during repeated-drop gameplay (e.g., second-drop near-bottom rotations).

## Demo stop control and fixed spawn id

- **Files:** `Sandbox/index.html`, `Sandbox/media-bridge.cjs`, `Sandbox/run-physics-tests.cjs`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/README.txt`
- **What:** Added stop/cancel control for live fill demo, set default demo spawn to triangle `16`, and adjusted demo tick pacing to `1000ms` with bridge support for `spawnId`.
- **Why:** Match requested gameplay flow (repeat same top-middle source) and allow stopping a running demo cleanly.

## Top-middle repeated drop gameplay flow

- **Files:** `Sandbox/index.html`, `Sandbox/media-bridge.cjs`, `Sandbox/run-physics-tests.cjs`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/README.txt`
- **What:** Added top-middle spawn helpers and repeat-drop-until-settle behavior for both live bridge commands and scripted tests, including a gameplay-style fill test case.
- **Why:** Support watching realistic repeated-drop gameplay where the same top-middle spawn is added repeatedly and settles to fill the container.

## Bridge command to load better tri

- **Files:** `Sandbox/media-bridge.cjs`, `Sandbox/index.html`, `Sandbox/README.txt`
- **What:** Added `loadContainerFile` bridge action plus `/load-container-file` endpoint, so chat/dispatch commands like “open better tri.json” can load the container into the already-open playground.
- **Why:** Let live interactive runs switch to the intended container file without manual file-picker interaction.

## Slower visual playback timing

- **Files:** `Sandbox/scenarios/physics-tests.json`
- **What:** Increased watch-mode delays (`stepDelayMs`, `tickDelayMs`, `testEndDelayMs`) to make per-step and per-tick movement easier to observe.
- **Why:** Improve human readability while watching tests run live in Chrome.

## Better-tri rule test retune and watchability

- **Files:** `Sandbox/scenarios/physics-tests.json`, `Sandbox/run-physics-tests.cjs`, `Sandbox/README.txt`, `Sandbox/index.html`
- **What:** Retuned rule tests for `better tri` IDs, added `nextTickLacksAnyPhase` assertions, and enabled visible watch mode with on-page status/delays while isolating automated runs from dispatch polling.
- **Why:** Keep suite meaningful for the selected container while making test progression observable and avoiding cross-talk from live command dispatch.

## Test runner isolated from dispatch queue

- **Files:** `Sandbox/index.html`, `Sandbox/run-physics-tests.cjs`
- **What:** Added `?nodispatch=1` support to disable bridge dispatch polling and applied it in the physics test runner page URL.
- **Why:** Prevent queued chat commands from contaminating automated test runs while still keeping dispatch polling enabled for normal interactive use.

## Visual test playback with status label

- **Files:** `Sandbox/run-physics-tests.cjs`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/README.txt`
- **What:** Added visible watch mode for physics tests with an in-page status label and configurable action/tick/end delays; enabled visualize defaults in the physics suite.
- **Why:** Make it easy to see which test is running and watch each tick progression while validating rules.

## Physics suite switched to better tri

- **Files:** `Sandbox/scenarios/physics-tests.json`, `Sandbox/find-phase-seeds.cjs`
- **What:** Pointed the default physics suite container to `Sandbox/better tri.json` and updated the phase-seed helper to accept `--containerFile`.
- **Why:** Run scripted physics tests against your intended container JSON instead of the previous default preset.

## Sandbox rule-focused phase tests

- **Files:** `Sandbox/scenarios/physics-tests.json`, `Sandbox/run-physics-tests.cjs`, `Sandbox/index.html`, `Sandbox/find-phase-seeds.cjs`, `Sandbox/README.txt`
- **What:** Added next-tick phase assertions and three rule-focused tests (lane, wall-tip-like, boundary-pivot-like), plus a helper script to discover stable seed IDs for target phases.
- **Why:** Enable repeatable checks tied to specific movement-rule phases rather than only end-state settle behavior.

## Sandbox per-test container JSON loading

- **Files:** `Sandbox/run-physics-tests.cjs`, `Sandbox/index.html`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/README.txt`
- **What:** Added support to load a specific container/state JSON per test via `containerFile` (or `loadContainerFile` action), with automation hook `loadObject` to apply JSON directly in headless runs.
- **Why:** Let each physics test declare exactly which container JSON to open before executing steps.

## Sandbox scripted physics test harness

- **Files:** `Sandbox/run-physics-tests.cjs`, `Sandbox/scenarios/physics-tests.json`, `Sandbox/index.html`, `Sandbox/README.txt`
- **What:** Added a headless pass/fail physics test runner with JSON-defined actions/assertions, plus automation hooks (`bottomIds`, `wouldMove`) used by scripted tests.
- **Why:** Make physics checks repeatable and runnable on demand from saved test files instead of manual clicking.

## Sandbox odd/even batch lighting commands

- **Files:** `Sandbox/media-bridge.cjs`, `Sandbox/index.html`
- **What:** Added command actions for lighting all odd- or even-numbered triangles and wired execution on the page dispatcher.
- **Why:** Support natural batch commands like “light all odd numbered triangles” directly from chat/bridge control.

## Sandbox dispatched command execution

- **Files:** `Sandbox/media-bridge.cjs`, `Sandbox/index.html`, `Sandbox/send-play-command.cjs`, `Sandbox/README.txt`
- **What:** Added dispatch queue endpoints (`/dispatch-command`, `/next-command`) and page polling so commands sent from CLI/chat are actually executed by an open playground page.
- **Why:** Close the control gap where chat commands only produced plans but did not apply actions in the live browser.

## Sandbox bridge command CLI

- **Files:** `Sandbox/send-play-command.cjs`, `Sandbox/README.txt`
- **What:** Added a small CLI utility that posts prompt text to the local `/play-command` bridge endpoint and prints returned mode/reply/actions.
- **Why:** Let Cursor chat trigger playground command plans quickly from terminal without needing Puppeteer or manual browser input.

## Sandbox bridge-backed command chat

- **Files:** `Sandbox/index.html`, `Sandbox/media-bridge.cjs`, `Sandbox/README.txt`
- **What:** Added bridge-backed command execution from the playground command box, with a new `/play-command` endpoint and optional LLM planning via API key plus local-parser fallback.
- **Why:** Enable no-Puppeteer text control from the page now, while allowing an upgrade path to model-driven command interpretation.

## Sandbox proof demo alignment

- **Files:** `Sandbox/index.html`
- **What:** Reworked `Proof demo` to seed the bottom four triangles dynamically and then add a center drop triangle before ticking the fall animation.
- **Why:** Keep demo behavior aligned with intended visuals even when triangle IDs drift as layout/mesh details evolve.
