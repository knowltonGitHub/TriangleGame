# Change log — 2026-04-15

Convention: one file per calendar day, name `CHANGELOG/YYYY-MM-DD.md`. Newest entries at the **top** (below this heading).

---

## One-line latest telemetry preview added

- **Files:** `Sandbox/index.html`
- **What:** Added a compact single-line `latest telemetry` preview above the collapsible telemetry section that updates live with each new telemetry event; bumped build to 96.
- **Why:** Let you glance at the newest message without expanding the full telemetry panel.

## Telemetry moved below board and collapsed by default

- **Files:** `Sandbox/index.html`
- **What:** Moved telemetry below the board/container section, wrapped it in a collapsed-by-default `<details>` toggle, and reduced telemetry panel height when expanded; bumped build to 95.
- **Why:** Keep gameplay/container visible sooner in the viewport while retaining telemetry access when needed.

## Sandbox accordion groups tinted pastel blue

- **Files:** `Sandbox/index.html`
- **What:** Updated the collapsible toolbar group cards to use a light pastel-blue background with a complementary border color.
- **Why:** Improve visual grouping and make accordion control areas easier to identify at a glance.

## Sandbox bridge-backed auto-refresh toggle

- **Files:** `Sandbox/index.html`, `Sandbox/media-bridge.cjs`
- **What:** Added an `Auto refresh` toggle in the Bridge toolbar group that watches `Sandbox/index.html` via a new bridge endpoint (`GET /page-version`) and reloads the page when the file changes; persisted toggle state in local storage; bumped build to 94.
- **Why:** Support side-by-side Cursor + Chrome workflow so UI edits appear automatically without manual browser refresh.

## Sandbox toolbar expand/collapse-all shortcuts

- **Files:** `Sandbox/index.html`
- **What:** Added compact `Expand all` and `Collapse all` toolbar controls that open/close every accordion group in one click; bumped build to 93.
- **Why:** Make it quick to reveal all controls when hunting for a button, then hide groups again to reduce clutter.

## Sandbox toolbar groups now collapsible

- **Files:** `Sandbox/index.html`
- **What:** Converted each toolbar section into a native collapsible accordion group (`<details>`/`<summary>`) with groups open by default and toggles for hiding unused controls; bumped build to 92.
- **Why:** Let active workflows stay visible while reducing clutter from groups you are not using in the moment.

## Sandbox toolbar regrouped by workflow

- **Files:** `Sandbox/index.html`
- **What:** Reorganized the top control bar into labeled groups (`Board`, `Container`, `Media & scenarios`, `Bridge & history`) and kept existing button IDs/actions intact; also bumped playground build to 91.
- **Why:** Make controls easier to scan and use by clustering related actions (load/save, media/scenarios, and runtime controls).

## Sandbox intro compressed with expandable notes

- **Files:** `Sandbox/index.html`
- **What:** Replaced the long top explanatory paragraph with a shorter summary line and added a native `<details>` toggle for expandable physics notes.
- **Why:** Keep core instructions visible while reducing visual clutter near the top of the playground.

## Sandbox control layout swap for faster board interaction

- **Files:** `Sandbox/index.html`
- **What:** Swapped the telemetry block and the main control toolbar in the page layout so telemetry renders above controls and the button row sits closer to the board container.
- **Why:** Improve interaction flow while filling and testing triangle panels by reducing visual distance between controls and the board.
