# Change log — 2026-04-17

Convention: one file per calendar day, name `CHANGELOG/YYYY-MM-DD.md`. Newest entries at the **top** (below this heading).

---

## Docs & Cursor rules: `playground.html` only (no `index.html` text)

- **Files:** `CHANGELOG/2026-04-13.md`, `CHANGELOG/2026-04-15.md`, `CHANGELOG/2026-04-16.md`, `.cursor/rules/lint-all-code.mdc`, `.cursor/rules/sandbox-physics-fallback.mdc`, `.cursor/rules/triangle-container-sandbox.mdc`, `Design/docs/GAME_DESIGN.md`
- **What:** Replaced remaining mentions of the old Sandbox HTML name with `playground.html` everywhere (including historical changelog lines and rule globs).
- **Why:** User asked to standardize on the filename they would have used from the start.

## Sandbox: `playground.html` entry + tooling paths

- **Files:** `Sandbox/playground.html`, `Sandbox/editor.html`, `Sandbox/js/playground.js`, `Sandbox/headless-preview.cjs`, `Sandbox/media-bridge.cjs`, `Sandbox/run-tick-scenario.cjs`, `Sandbox/run-physics-tests.cjs`, `Sandbox/find-phase-seeds.cjs`, `Sandbox/open-in-chrome.bat`, `Sandbox/README.txt`, `Sandbox/SESSION_HANDOFF.md`, `Sandbox/capture-container-T-rotations.cjs`, `Sandbox/crash-fall-demo.json`
- **What:** Playground loads from `playground.html`; scripts, bridge defaults (`/page-version` path), Playwright URLs, telemetry source strings, and docs use that path. Playground build **150**.
- **Why:** User request to use `playground.html` as the HTML entry name everywhere.

## Sandbox: playground script moved to `js/playground.js`

- **Files:** `Sandbox/playground.html`, `Sandbox/js/playground.js`, `Sandbox/headless-preview.cjs`
- **What:** Moved the large inline IIFE from the playground HTML into `js/playground.js`; the page loads it after `presets/playground-default-presets.js`. Headless preview executes that file directly instead of regex-parsing HTML.
- **Why:** User request to keep JavaScript in a `js` folder next to the playground HTML.
