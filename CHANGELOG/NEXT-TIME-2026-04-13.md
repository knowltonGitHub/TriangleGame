# Next time — 2026-04-13

Purpose: capture items discussed but not yet implemented, so the next coding session can resume quickly.

---

## Open items (not implemented yet)

- [ ] Move candidate scoring constants and tie-break weights from JavaScript into JSON rule data so behavior tuning is fully data-driven.
- [ ] Expand the JSON schema to cover all per-state scoring knobs and thresholds (including wall-mode weighting) with validation.
- [ ] Add rule-focused tests that prove changing rule JSON changes movement outcome for the same seed/setup.
- [ ] Define an explicit "state catalog" document for all fall/slide/rotate states and required neighbor-visibility inputs.

## Starting point for next session

- Current engine uses JSON rule ordering/conditions (`requires`, `forbids`) including wall FSM states.
- Full physics suite passes with `Sandbox/rules/triangle-physics.rules.v1.json`.
- Recommended first task next time: implement JSON-driven scoring constants and add one A/B test case that flips expected movement by editing only the rule file.
